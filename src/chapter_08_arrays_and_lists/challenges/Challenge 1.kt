package chapter_08_arrays_and_lists.challenges

/***
 * 1. Which of the following 1-10 are valid statements?
 *          1. val array1 = Array<Int>()                            -> Invalid
 *          2. val array2 = arrayOf()                               -> Invalid
 *          3. val array3: Array<String> = arrayOf()                -> Valid
 *    For the next three statements, array4 has been declared as:
 *      val array4 = arrayOf(1, 2, 3)
 *          4. println(array4[0])                                   -> Valid
 *          5. println(array4[5])                                   -> Invalid
 *          6. array4[0] = 4                                        -> Valid
 *    For the final five statements, array5 has been declared as:
 *      val array5 = arrayOf(1, 2, 3)
 *          7. array5[0] = array5[1]                                -> Valid
 *          8. array5[0] = "Six"                                    -> Invalid
 *          9. array5 += 6                                          -> Invalid
 *          10. for item in array5 { println(item) }                -> Invalid
 */
